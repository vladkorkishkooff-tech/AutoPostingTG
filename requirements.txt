# Exact top-level versions keep Docker/Railway builds reproducible. Update
# deliberately and rerun the complete test/build gates before changing them.
aiogram==3.29.1
python-dotenv==1.2.2
aiohttp==3.14.1
aiohttp-socks==0.11.0
asyncpg==0.31.0
cryptography==49.0.0
